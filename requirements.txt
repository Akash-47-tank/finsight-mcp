# FinSight MCP — Python Dependencies
# Install with: pip install -r requirements.txt

# MCP SDK (official Anthropic SDK for Model Context Protocol)
mcp>=1.0.0

# Stock market data (free, no API key needed)
yfinance>=0.2.40

# HTTP client (for Ollama + NewsAPI calls)
httpx>=0.27.0

# Terminal UI (beautiful output in the client)
rich>=13.0.0

# Environment variable management
python-dotenv>=1.0.0
